# Metrics

性能统计层。

后续放置：

```text
metrics_collector.h/.cpp
fps_counter.h
latency_tracker.h
metrics_snapshot.h
```

需要统计：

```text
capture_fps
capture_ms
encode_ms
send_queue_depth
dropped_frames
bitrate
rtp_jitter
end_to_end_latency
```
